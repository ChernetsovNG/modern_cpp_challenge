Для сборки всех проектов при помощи CMake:

1) Создаём папку для файлов сборки

`mkdir build`

2) Генерируем файлы для сборки проекта

`cmake .. -G "MinGW Makefiles"`

3) Генерируем исполняемый файл программы

`cmake --build .`
